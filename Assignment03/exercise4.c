#include <stdio.h>
#include <string.h>
#define N 100
#define M 13

void scanSentence(int input[N]) {
    for (int i = 0; i < N; i++) {
        scanf("%s", &input[i]);
    }
    return;
}

int main() {
    char input[N][M];
    int hist[M];

    // if ((65 <= character && character <= 90) || (97 <= character && character <= 122))

    return 0;
}
